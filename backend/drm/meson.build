if not gbm.found()
  subdir_done()
endif

wlr_files += files(
	'atomic.c',
	'backend.c',
	'cvt.c',
	'drm.c',
	'legacy.c',
	'properties.c',
	'renderer.c',
	'util.c',
)

features += {'drm-backend': true}
